$(document).ready(function() {
	$('#btnRecipes').click(function() {
		alert("Thanks for clicking but this is all I can do now.  Come back soon for actual recipes!");
		//Make a call to the API and display the results
		// var recipes;
		// recipes = curl "https://api.edamam.com/search?q=chicken&app_id=${YOUR_APP_ID}&app_key=${YOUR_APP_KEY}"
		// console.log(recipes);
	});
});